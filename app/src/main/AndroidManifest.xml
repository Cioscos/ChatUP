<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.chatup">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_chatup"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_chatup_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/AdMob_app_id"/>

        <activity android:name="com.example.chatup.RegisterActivity"/>
        <activity android:name="com.example.chatup.MainActivity" />
        <activity android:name="com.example.chatup.LoginActivity"/>
        <activity android:name="com.example.chatup.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>